import React from 'react';
interface SanityStructuredTextProps {
    data?: any;
    className?: string;
    as?: string;
}
declare const StructuredText: ({ data, className, as}: SanityStructuredTextProps) => JSX.Element;
export default StructuredText;
